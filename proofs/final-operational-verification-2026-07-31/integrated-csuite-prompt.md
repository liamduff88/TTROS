You are the live `aos-orchestrator` profile performing the final bounded Agentic OS acceptance run. Work only inside `/home/liam/agentic-os-live`. Do not use the global/default Hermes profile. Do not send, publish, connect, mutate any connector, inspect a live client workspace, expose secrets, commit, or push.

1. Retrieve current global Business Brain context with `python3 tools/business_brain_context.py --client-scope global --query "Time to Revenue priorities positioning offers ideal clients delivery operations" --graphify --limit 8`. Use its scoped actual-read provenance in the work below. Do not print note bodies or sensitive values.
2. Use the terminal to invoke `tools/aos-hermes-coordinator.sh` exactly once for each profile below. Supply `--usage-file` and save stdout to the exact output path. Give every child the relevant non-sensitive context from step 1, require a useful compact artifact rather than a marker, require a `Token usage:` line, and forbid tools/external actions.
   - `aos-revenue`: rank the two already reviewable opportunities from the current bounded prospecting proof, preserve duplicate/prior-contact and human-review gates, and state the best next commercial action. Output `proofs/final-operational-verification-2026-07-31/integrated-revenue.md`; usage `proofs/final-operational-verification-2026-07-31/integrated-revenue-usage.json`.
   - `aos-marketing`: draft one concise LinkedIn post aligned to the current positioning/voice that supports the Revenue angle, clearly marked draft-only. Output `proofs/final-operational-verification-2026-07-31/integrated-marketing.md`; usage `proofs/final-operational-verification-2026-07-31/integrated-marketing-usage.json`.
   - `aos-delivery`: using only `proofs/final-operational-verification-2026-07-31/safe-delivery-fixture.md` plus the supplied global delivery context, provide a compact first-week acceptance checklist. Output `proofs/final-operational-verification-2026-07-31/integrated-delivery.md`; usage `proofs/final-operational-verification-2026-07-31/integrated-delivery-usage.json`.
   - `aos-ops`: coordinate those three workstreams into today's ordered focus, review gates, owner handoffs, and follow-up cadence. Output `proofs/final-operational-verification-2026-07-31/integrated-operations.md`; usage `proofs/final-operational-verification-2026-07-31/integrated-operations-usage.json`.
3. Verify all four output and usage files exist and contain the expected useful work and profile usage evidence. Then synthesize a compact integrated operating answer for Liam: what to focus on first, how Revenue/Marketing/Delivery/Operations support one another, what awaits human review, and confirmation of zero external actions and no default fallback. Include actual-read provenance paths/hashes without note bodies. End with `INTEGRATED_ACCEPTANCE: PASS` and a `Token usage:` section.
