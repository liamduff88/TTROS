# Phase 2A — declared change and rollback (written before editing, per CLAUDE.md rule 4)

## Exact change

1. `hooks/validation_a_seal_guard.py` — add a second sealed-path source, loaded fresh (no
   caching) on every `evaluate()` call exactly like the existing manifest load: the 14 entries of
   `scripts/validation_a_runs/blind_run_phase1_freeze.json` → `distinct_resolved_source_paths`,
   plus their basenames. Merged into the same deny-set as the existing 46-path
   `seal_manifest.json` source. Same `TTROS_VALIDATION_A_RUN` env gate. No change to
   `BLOCKED_TOOL_NAMES` or to the existing manifest-loading code path.
2. `tests/test_validation_a_seal_guard.py` — add cases proving, live, both directions on the
   extension specifically: one of the 14 freeze-file source paths is DENIED while the run is
   active; the existing unsealed control path is still ALLOWED while active; the extension is
   inert (no denial) when `TTROS_VALIDATION_A_RUN` is unset.
3. A standalone rehearsal transcript,
   `scripts/validation_a_runs/phase2a_guard_extension_rehearsal.txt`, generated by directly
   calling `evaluate()` (mirrors `phase0_guard_rehearsal.txt`'s method), teed once, not
   overwritten.

Both edited files are untracked in git (`git status --porcelain` confirms `??` for both), so no
git rollback is available for either — per CLAUDE.md rule 6, immutable preimages were saved
first, before this file was written.

## Rollback

Preimages (sha256 recorded in `SHA256SUMS` alongside them):

```
/home/liam/ttros_backups/validation_a_phase2_preimages_20260909T075922Z/validation_a_seal_guard.py.preimage
/home/liam/ttros_backups/validation_a_phase2_preimages_20260909T075922Z/test_validation_a_seal_guard.py.preimage
/home/liam/ttros_backups/validation_a_phase2_preimages_20260909T075922Z/detector.py.preimage
```

(`detector.py.preimage` captured here too, ahead of Phase 2B's full rewrite of that file, so both
declarations share one preimage set and one rollback command block.)

Rollback command, if needed:

```
cp /home/liam/ttros_backups/validation_a_phase2_preimages_20260909T075922Z/validation_a_seal_guard.py.preimage hooks/validation_a_seal_guard.py
cp /home/liam/ttros_backups/validation_a_phase2_preimages_20260909T075922Z/test_validation_a_seal_guard.py.preimage tests/test_validation_a_seal_guard.py
cp /home/liam/ttros_backups/validation_a_phase2_preimages_20260909T075922Z/detector.py.preimage scripts/validation_a_runs/blind_run/detector.py
```

## Scope

Local, reversible file edits only. No git add/commit/push. No network. No model call. No vault
write. Proceeding without further pause per CLAUDE.md rule 4/5 (scoped reversible local work this
step already authorizes).
