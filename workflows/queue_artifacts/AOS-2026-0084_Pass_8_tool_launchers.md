# Pass 8 — tool launchers and honest status
> Revisit: when launcher binaries or status contracts change. · Last touched: 2026-07-12.

PASS

- Verified Hermes dashboard path: dashboard API → `tools/aos-hermes-dashboard.sh` → 127.0.0.1:8081 readiness. Current status is honestly reachable under `/home/liam/agentic-os-live`.
- Graphify status checks only the installed CLI/version and explicitly does not inspect Graphify Brain data.
- Latitude remains additive status-only observability with its current configured/connected state and no payload expansion.
- Codex and Claude Code copy prompts preserve the exact scoped permission header and Linux-root launch scaffolds; byte checks pass.
- AgentMail is status-only and labelled `internal-live` for olmec1@agentmail.to → liam@timetorevenue.com.
- Telegram is instructions/copy only; the prior `tg://` and Windows Start launcher assumptions were removed.
- Browser proof: `workflows/queue_artifacts/pass8-proof/browser-proof.json` and two screenshots.

Token usage: unavailable from current CLI output.
