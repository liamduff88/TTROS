# Promotion authority tier report — Block 2

Status: **PASS**

The existing `context/MEMORY_PROMOTION_POLICY.md` now defines executable authority tiers. Only one automatic class is enabled: `generated_marker_section`, limited to `business_brain:index/MEMORY_INDEX.md` and an exact declared machine-owned marker. It refuses text outside the marker, is hash-checked/idempotent, and writes a receipt without a queue item.

Positioning, pricing/offers, commitments, communications-derived facts, strategy, legal/financial conclusions, architecture/authority/protected changes, policies, conflicts, deletion, and supersession always route to `human_review`. Review proposals require canonical target, preimage hash, candidate diff, provenance, scope, and reason; they cannot write without an acceptance reference.

Secrets, credentials/auth material, runtime state/logs, raw receipt/artifact trees, speculation-as-fact, source trees, raw Graphify output, raw communications, and out-of-scope protected information are refused with no writable diff. Unknown classes fail closed to review.

Automated proofs cover enabled automatic behavior, review routing and acceptance guards, never-promote refusal, unknown-to-review, cross-client denial, stale hashes, target/marker restrictions, duplication, rollback, and linkage failures.

Token usage: no agent invocation
