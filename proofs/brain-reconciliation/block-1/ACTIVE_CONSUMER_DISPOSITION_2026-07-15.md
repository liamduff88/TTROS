# Block 1 active Business Brain consumer disposition

Date: 2026-07-15
Authority baseline: HEAD/main/origin-main `8b26010772196c448e01fae4dacfbe4ef9106c4b`
Token usage: unavailable from current CLI output.

| Consumer or convention | Live finding | Block 1 disposition |
|---|---|---|
| Runtime pointer resolution | Consumers had forked roots or documentation-only relative names. | Repaired with `tools/business_brain.py`. The only physical root constant is there; all runtime resolution uses `business_brain:<relative-path>`, canonical POSIX normalization, vault containment, file existence, and `_backups` rejection. No basename lookup exists. |
| `tools/aos_indexer.py` and local search | Brain root was derived from the repo parent and could scan backups. | Repaired to import the shared root/projector, exclude `_backups`, and emit logical Brain paths. Proven with a disposable SQLite index; the live `search/os_index.db*` was not rebuilt or changed. |
| `context/MEMORY_ROOT.md`, `memory_index/README.md`, `memory_index/INDEX.md` | Repo-local pointer pages mixed stale paths and noncanonical conventions. | Repaired as pointer-only navigation maps to canonical logical targets. They do not embed a Windows or WSL path. |
| Workbench identities (`CODEX.md`, `CLAUDE.md`, `ANTIGRAVITY.md`) | `graph-imports/aos-repo` and `_substrate.wiki` were presented as live Brain routes. | Those conventions are retired. Each identity starts at `business_brain:index/MEMORY_INDEX.md` and requests specific logical targets only. |
| Orchestrator/department identities | Revenue, Delivery, Ops, Marketing, and `soul.md` used mixed numeric IDs, prose names, or old pointer conventions. | Repaired to explicit canonical pointers. Marketing's `memory 012/014/015` convention is retired. |
| Standing goal and verifier | The goal checked `_substrate.wiki/schema.md` through a literal physical path. | Repaired so `loop/verify-goals.sh` invokes the shared resolver for the Brain root and index. The focused standing-goal check passes. |
| Repository context/rules | `OPERATING_BASELINE`, archive plan, protected-path reference, always rule, and client-boundary rule did not share one convention. | Repaired to logical pointers. Client reads require an exact full pointer; unresolved scope stops before loading. Until Block 2, legacy entity-page update wording means a receipt proposal, not a Brain write. |
| Queue prompts/templates | Active work-order and promotion proposal examples used ambiguous relative target names. | Repaired to logical pointers. The promotion template remains proposal-only; no queue item, promotion evaluator, caller, or writer was created. Dispatcher/outreach templates already route through `MEMORY_ROOT`/`memory_index` and were retained. |
| Registered skills and workflows | Content, outreach, proposal, fit-call, weekly-review, carousel, marketing, and promotion instructions used bare `memory/...`, bare `operating_context/...`, or inferred entity-page names. | Direct knowledge references were converted to logical pointers. Client-specific reads require an exact pointer supplied by the work item; no client registry or scoped loader was added. Fit-call/outreach entity changes are receipt proposals only at checkpoint 1. |
| `/api/dashboard/memory` | It read repository-local memory/context as if it were the Brain and inferred a promotion queue from queue titles/tags. | Repaired to read the canonical vault through the shared resolver, return 19 logical note pointers, report 18 excluded backups, and expose promotion as explicitly inactive. The inferred promotion queue and all frontend presentation of it are retired. |
| Dashboard Memory Board | The caller rendered the misleading route without canonical state. | Repaired to show Brain availability, canonical note count, backup exclusion, exact pointers/types, and inactive promotion state. Real backend and browser proofs recorded. |
| Vault navigation and Obsidian | Canonical notes lacked stable IDs; roots were not complete navigation; graph filter did not exclude backups. | Repaired with minimal `id`/`type` frontmatter and root wiki links. `README.md` and `index/MEMORY_INDEX.md` reach every canonical note within one hop. `_backups` is excluded from graph/navigation. |
| Business Brain Graphify selector | No maintainable documents-mode TTROS projection met identity, link, target, freshness, or failure-preservation contracts. | Added `BusinessBrainGraphService` and a thin installed-package structural extractor wrapper. Namespace is `/home/liam/graphify-brain/document_graphs/ttros-business-brain`; output is derived and never authoritative. |
| Graphify package documents CLI | Graphify 0.9.11 discovered Markdown but required an external LLM key; its local extractor mis-resolved some Obsidian root-style links. | Package left unchanged. TTROS invokes its installed deterministic structural extractor, then resolves links with the vault validator's exact parser and marks package-confirmed versus TTROS-repaired explicit edges. |
| Existing Repo Ingest / interactive Graphify | Existing Pass 10 uses `intake/`, `repo_graphs/`, and `receipts/`. | Retained as a separate code-graph surface. All three before/after manifests are byte-identical and the 24-test Pass 10 suite remains green. |
| `_backups/` | Could leak through search, dashboard, pointer resolution, Obsidian graph, navigation, or Graphify source selection. | Rejected/excluded at every active boundary above. Backups remain physically untouched and are not canonical targets. |
| Historical receipts, outputs, packets, design notes | Some accurately record old literal paths or old Graphify states. | Classified as historical evidence, not active consumers. Preserved without rewriting. |
| Promotion execution | Existing policy/skill prose describes future reviewed promotion behavior. | Logical target examples were normalized, but promotion execution is inactive. No promotion policy redesign, authority tier, writer, or first write was implemented. |

Authority-document discovery note: `results/system_readouts/TTROS_SYSTEM_READOUT_2026-07-15.md` was found and read completely. The requested exact filenames `TTROS_BRAIN_RECONCILIATION_LOCKED_DESIGN_2026-07-15.md`, `TTROS_SYSTEM_CONTEXT_CURRENT_STATE_2026-07-15.md`, and `TTROS_HISTORY_EVIDENCE_AND_TRANSFER_INDEX_2026-07-15.md` were absent after repository, canonical-vault, `/tmp`, shallow `/home/liam`, and Windows-parent filename searches. The adopted runbook, adopted packaging amendment, July 15 readout, and newer live proofs controlled the implementation; no duplicate authority files were invented.
